# 🌌 动态壁纸生成器 | Dynamic Wallpaper Generator

🖼️ 将 MP4 视频一键打包为可导入模组！  
🖼️ One-click tool to convert MP4 videos into ready-to-import wallpaper mods!

---

## 📂 使用方法 | How to Use

1. 将你想要转换的 MP4 视频放入 `PlaceYourMp4Here` 文件夹。  
   Put the MP4 videos you want to convert into the `PlaceYourMp4Here` folder.

2. 双击运行 `ClickHere.bat`。  
   Double-click `ClickHere.bat` to start the program.

3. 程序会自动处理前 **30 个视频**，并生成最终的 `DynamicWallpaper.zip` 文件。  
   The program will automatically process the **first 30 videos**, then generate a final `DynamicWallpaper.zip`.

4. 将 `DynamicWallpaper.zip` 导入你的游戏或模组管理器（如 Vortex）。  
   Import `DynamicWallpaper.zip` into your game or mod manager (e.g., Vortex).

---

## ⚠ 注意事项 | Notes

- **⚠ 请确保路径中不包含中文字符，否则可能导致生成失败。**  
  **⚠ Make sure your folder path does not contain any Chinese characters, or the process may fail.**

- 每个视频会自动压缩、裁剪并转换为兼容格式。  
  Each video will be automatically compressed, cropped, and converted.

- **⚠ 程序运行时会弹出控制台窗口，请勿随意拖动、缩放或最小化，以免影响进度条显示。**  
  **⚠ A console window will pop up during execution — do not move, resize, or minimize it to avoid breaking the progress display.**


## 🧾 模组作者 | Mod Author

- 作者：**沐子云**  
  Author: **Muziyun**

- 哔哩哔哩：**林夕主儿**  
  Bilibili: **林夕主儿**

- 哔哩哔哩主页：  
  Bilibili Profile:  
   https://space.bilibili.com/8927484
